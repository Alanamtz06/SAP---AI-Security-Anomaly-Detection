applications:
  - name: alerting-service
    memory: 256M
    instances: 1
    timeout: 300
    command: python app.py --daemon
    no-route: true
    health-check-type: process
    env:
      ALERT_INTERVAL_SECONDS: 60
    services:
      - DB_Hack
