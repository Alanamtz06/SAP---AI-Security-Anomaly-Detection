applications:
  - name: soc-etl-pipeline
    memory: 512M
    instances: 1
    timeout: 600
    command: python app.py --daemon
    no-route: true
    health-check-type: process
    env:
      POLL_INTERVAL_SECONDS: 120
      ML_ENGINE_URL: https://ml-engine.cfapps.us10.hana.ondemand.com
    services:
      - DB_Hack
